beforeAll(() => {
  // do something before all
})

beforeEach(() => {
  // Do something before each test
})

describe('Some test', () => {
  test('Runs test', async () => {
    expect(true).toBe(true)
  })
})
